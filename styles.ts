import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bannerAdContainer: {
        flexDirection: "row",
        justifyContent: "center",
        marginVertical: 10
    }
})
